<Window x:Class="PinyinLexTool.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PinyinLexTool.WPF"
        mc:Ignorable="d"
        Title="微软拼音自定义短语工具" Height="600" Width="900" 
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="10,5"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="微软拼音输入法自定义短语批量导入/导出工具" 
                       FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="当前词库文件路径：" Margin="0,5,0,0"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding LexFilePath}" IsReadOnly="True"/>
                <Button Grid.Column="1" Content="浏览..." Click="BrowseLexFile_Click"/>
            </Grid>
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="导入短语">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding ImportFilePath}" IsReadOnly="True"/>
                        <Button Grid.Column="1" Content="选择文件..." Click="BrowseImportFile_Click"/>
                    </Grid>
                    
                    <TextBox Grid.Row="1" Margin="5" 
                             Text="{Binding ImportContent, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas, Courier New, monospace"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                        <CheckBox Content="创建备份" IsChecked="{Binding CreateBackup}"/>
                        <CheckBox Content="仅校验（不写入）" IsChecked="{Binding DryRun}" Margin="15,5,5,5"/>
                        <CheckBox Content="显示详细信息" IsChecked="{Binding Verbose}" Margin="15,5,5,5"/>
                        <Button Content="导入" Click="Import_Click" HorizontalAlignment="Right" Margin="15,5,5,5"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            
            <TabItem Header="导出短语">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding ExportFilePath}" IsReadOnly="True"/>
                        <Button Grid.Column="1" Content="选择文件..." Click="BrowseExportFile_Click"/>
                    </Grid>
                    
                    <TextBox Grid.Row="1" Margin="5" 
                             Text="{Binding ExportContent}"
                             IsReadOnly="True"
                             AcceptsReturn="True" TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas, Courier New, monospace"/>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal">
                        <Button Content="导出" Click="Export_Click"/>
                        <Button Content="复制到剪贴板" Click="CopyExport_Click" Margin="15,5,5,5"/>
                    </StackPanel>
                </Grid>
            </TabItem>
            
            <TabItem Header="查看短语">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="拼音过滤：" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Grid.Column="2" Content="查询" Click="List_Click"/>
                    </Grid>
                    
                    <DataGrid Grid.Row="1" Margin="5" 
                              ItemsSource="{Binding PhraseList}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#F5F5F5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="拼音" Binding="{Binding Pinyin}" Width="100"/>
                            <DataGridTextColumn Header="位置" Binding="{Binding Index}" Width="50"/>
                            <DataGridTextColumn Header="短语文本" Binding="{Binding Text}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
        
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
